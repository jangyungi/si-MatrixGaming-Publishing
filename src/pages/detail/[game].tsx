import { DetailView } from "@/domains/Detail/view";
import type { NextPage } from "next";
import React from "react";

const DetailPage: NextPage = () => {
  return (
    <React.Fragment>
      <DetailView />
    </React.Fragment>
  );
};

export default DetailPage;
